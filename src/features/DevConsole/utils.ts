// Utils for colors
export function isBright(hex: string) {
    // Very rough heuristic
    return hex === '#ffffff' || hex === '#f1c40f' || hex === '#2ecc71';
}

export function lighten(color: string, _amount: number) {
    // Placeholder interaction
    return color; 
}
