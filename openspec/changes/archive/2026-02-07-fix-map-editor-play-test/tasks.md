## 1. App Updates

- [x] 1.1 Update `LevelEditorPage` in `App.tsx` to pass `customMapData` in navigation state.
- [x] 1.2 Update `GamePage` in `App.tsx` to read `customMapData` from location state.
- [x] 1.3 Update `GamePage` in `App.tsx` to pass `customMapData` back when returning to editor.
- [x] 1.4 Update `LevelEditorPage` in `App.tsx` to use `initialData` from location state.

## 2. Component Cleanup

- [x] 2.1 Rename `handleExport` to `handlePlay` in `LevelEditor/index.tsx` for clarity (optional but recommended).
