## 1. App Component Restriction

- [x] 1.1 Conditionally pass `onOpenLevelEditor` to `CharacterSelect` in `src/App.tsx` using `import.meta.env.DEV`.

## 2. Verification

- [x] 2.1 Verify "Level Editor" button is visible in development mode.
- [x] 2.2 Verify "Level Editor" button is hidden when `import.meta.env.DEV` is falsy.
