## 1. UI Refactoring

- [x] 1.1 Extract or create reusable `MapModal` components (Save/Load)
- [x] 1.2 Implement `SaveModal` state in `LevelEditor`

## 2. Save Logic Enhancement

- [x] 2.1 Update `handleSaveMap` to trigger `SaveModal` instead of `window.prompt`
- [x] 2.2 Populate map list in `SaveModal` (reuse map fetching logic)
- [x] 2.3 Implement name selection from list (click to populate input)
- [x] 2.4 Add overwrite confirmation logic

## 3. UI Styling

- [x] 3.1 Style `SaveModal` to match `LoadModal` and existing Level Editor theme
- [x] 3.2 Ensure responsive behavior for long map lists
