# Tasks: Go to Spawn Point

## 1. Implementation

- [x] 1.1 Add `onToolDoubleClick` prop to `EditorSidebar` and pass it down to `ToolCard`.
- [x] 1.2 Implement `handleToolDoubleClick` in `LevelEditor` to calculate scroll position and perform smooth scroll.
- [x] 1.3 Trigger `onToolDoubleClick` when double-clicking the Spawn tool in `EditorSidebar`.

## 2. Verification

- [x] 2.1 Verify double-clicking Spawn tool centers the view on the spawn point.
- [x] 2.2 Verify nothing happens if no spawn point is set.
